export interface GameProperties {
  imageUrl: string;
  provider: string;

  // Slot Catalog data
  slot_rank: number;
  release_date: string;
  game_technology: string;
  game_size: string;
  genre: string;
}
